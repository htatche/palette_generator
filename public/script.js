// Main application initialization

// Initialize the app when DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
    console.log('🚀 DOM loaded, initializing Color Palette Generator...');
    new PaletteGenerator();
});
